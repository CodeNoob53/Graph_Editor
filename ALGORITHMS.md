# Додані алгоритми

Цей документ описує нові алгоритми, додані до Graph Editor.

## 1. Topological Sort (Топологічне сортування)

**Файл:** `src/algorithms/topologicalSort.js`

**Функції:**
- `topologicalSort(cy, isDirected)` - DFS-based топологічне сортування
- `topologicalSortKahn(cy, isDirected)` - Алгоритм Кана (використовує вхідні степені)

**Застосування:**
- Працює тільки для орієнтованих ациклічних графів (DAG)
- Повертає порядок вершин, де для кожного ребра (u, v), вершина u йде перед v
- Корисно для планування задач, визначення порядку компіляції тощо

**Підсвітка:** використовує `highlightPath()` для відображення топологічного порядку

---

## 2. SCC - Tarjan's Algorithm (Сильно зв'язні компоненти)

**Файл:** `src/algorithms/scc.js`

**Функції:**
- `findStronglyConnectedComponents(cy, isDirected)` - Алгоритм Тарьяна
- `isStronglyConnected(cy, isDirected)` - Перевірка сильної зв'язності
- `findCondensation(cy, isDirected)` - Конденсація графу

**Застосування:**
- Працює тільки для орієнтованих графів
- Знаходить максимальні множини вершин, між якими є взаємна досяжність
- Використовується в аналізі соціальних мереж, оптимізації компіляторів

**Підсвітка:** виводить інформацію про компоненти в текстовому вигляді

---

## 3. PageRank

**Файл:** `src/algorithms/pagerank.js`

**Функції:**
- `calculatePageRank(cy, isDirected, dampingFactor, maxIterations, tolerance)`
- `getTopPageRankNodes(cy, topN, isDirected, dampingFactor)` - Топ-N вершин
- `calculatePersonalizedPageRank(cy, startNodeId, isDirected, dampingFactor, maxIterations)`

**Застосування:**
- Ранжування вершин графу за важливістю
- Алгоритм Google для ранжування веб-сторінок
- Працює як для орієнтованих, так і для неорієнтованих графів

**Параметри:**
- `dampingFactor` (за замовчуванням 0.85) - ймовірність переходу по ребру
- `maxIterations` (за замовчуванням 100) - максимальна кількість ітерацій
- `tolerance` (за замовчуванням 0.0001) - точність збіжності

**Підсвітка:** виводить топ-10 вершин за PageRank

---

## 4. Bridges & Articulation Points (Мости та точки зчленування)

**Файл:** `src/algorithms/bridgesAndArticulation.js`

**Функції:**
- `findBridges(cy, isDirected)` - Пошук мостів
- `findArticulationPoints(cy, isDirected)` - Пошук точок зчленування
- `findBridgesAndArticulationPoints(cy, isDirected)` - Комбінований алгоритм

**Застосування:**
- Працює для неорієнтованих графів
- **Міст** - ребро, видалення якого збільшує кількість компонент зв'язності
- **Точка зчленування** - вершина, видалення якої збільшує кількість компонент
- Використовується в аналізі надійності мереж

**Підсвітка:** використовує `highlightEdges()` для підсвітки мостів

---

## 5. A* (A-star) - Алгоритм з евристикою

**Файл:** `src/algorithms/astar.js`

**Функції:**
- `findPathAStar(cy, sourceId, targetId, isDirected, heuristic)`
- `compareAStarWithDijkstra(cy, sourceId, targetId, isDirected)` - Порівняння з Діскрою
- `findNearestTarget(cy, sourceId, targetIds, isDirected, heuristic)` - Найближча ціль

**Застосування:**
- Знаходить найкоротший шлях між двома вершинами
- Ефективніший за Дейкстру через використання евристики
- Показує кількість досліджених вершин

**Евристики:**
- `euclidean` (за замовчуванням) - Евклідова відстань
- `manhattan` - Манхеттенська відстань

**Підсвітка:** використовує `highlightPath()` для відображення знайденого шляху

---

## 6. Kruskal's Algorithm (MST)

**Файл:** `src/algorithms/kruskal.js`

**Функції:**
- `calculateKruskalMST(cy, isDirected)` - Алгоритм Крускала
- `compareMSTAlgorithms(cy)` - Порівняння з Прімом
- `calculateMaximumSpanningTree(cy, isDirected)` - Максимальне остовне дерево

**Застосування:**
- Працює для неорієнтованих зв'язних графів
- Знаходить мінімальне остовне дерево (MST)
- Використовує Union-Find структуру для ефективного об'єднання множин
- Edge-based підхід (на відміну від Пріма - vertex-based)

**Підсвітка:** використовує `highlightEdges()` для відображення MST

---

## 7. Bellman-Ford Algorithm

**Файл:** `src/algorithms/bellmanFord.js`

**Функції:**
- `bellmanFord(cy, sourceId, isDirected)` - Шляхи до всіх вершин
- `findPathBellmanFord(cy, sourceId, targetId, isDirected)` - Шлях між двома вершинами
- `detectNegativeCycles(cy, isDirected)` - Виявлення від'ємних циклів
- `findLongestPathInDAG(cy, sourceId, targetId, isDirected)` - Найдовший шлях в DAG

**Застосування:**
- Знаходить найкоротші шляхи від вихідної вершини
- На відміну від Дейкстри, може працювати з **від'ємними вагами**
- Виявляє від'ємні цикли в графі
- Працює як для орієнтованих, так і для неорієнтованих графів

**Підсвітка:** використовує `highlightPath()` для відображення шляху

---

## Паттерни використання

### isDirected
Всі алгоритми приймають параметр `isDirected`:
- При побудові списку суміжності завжди додається пряме ребро
- Для неорієнтованого графу додається зворотне ребро

### Підсвітка
Залежить від типу результату:
- `highlightPath(cy, path, isDirected)` - для масиву вершин
- `highlightEdges(cy, edges, isDirected)` - для масиву ребер
- `highlightNodesAndEdges(cy, nodes, edges, isDirected)` - для обох
- `clearHighlights(cy)` - очищення підсвітки

### Структура результату
Всі алгоритми повертають об'єкт з такою структурою:
```javascript
{
  success: true,           // або error для помилок
  algorithm: 'Назва',      // назва алгоритму
  graphType: 'орієнтований' | 'неорієнтований',
  // ... специфічні дані алгоритму
}
```

---

## UI Інтеграція

Всі алгоритми інтегровані в UI через:
1. **HTML кнопки** в `index.html` (секція `rpContent`)
2. **Event handlers** в `src/modules/uiHandlers.js`
3. **Імпорти** алгоритмів на початку `uiHandlers.js`

### Структура UI (Організована по розділах):

#### 📊 Graph Information
- `getInfo` - Get Graph Info
- `checkConnectivity` - Check Graph Connectivity
- `detectCycle` - Detect Cycles

#### 🔍 Path & Flow Algorithms (Спойлери)

**🌳 MST Algorithms** (спойлер):
- `calculateMST` - MST (Prim's Algorithm)
- `calculateKruskalMST` - MST (Kruskal's Algorithm)

**🛤️ Shortest Path & Flow** (спойлер):
- `findPath` - Dijkstra (Shortest Path)
- `runAStar` - A* Algorithm
- `runBellmanFord` - Bellman-Ford
- `runMaxFlow` - Max Flow (Edmonds-Karp)

**🔄 Graph Traversal** (спойлер):
- `runDFS` - DFS (Depth-First Search)
- `runBFS` - BFS (Breadth-First Search)

#### ⚙️ Graph Algorithms
- `runTopologicalSort` - Topological Sort
- `findSCC` - Strongly Connected Components
- `calculatePageRank` - PageRank
- `findBridgesAndArticulation` - Bridges & Articulation Points

#### 🔁 Paths & Cycles
- `findMinPath` - Min Weighted Path (4 Vertices)
- `listSpanningTrees` - All Spanning Trees
- `eulerCalculation` - Euler Trails & Circuits
- `hamiltonianCalculation` - Hamiltonian Cycles

### Додані стилі:
- `.section-title` - Заголовки розділів з синім акцентом VS Code
- Оновлені стилі для всіх спойлерів (`#mstButtons`, `#pathInputs`, `#traversalInputs`)

---

## Тестування

Рекомендовані тестові графи:

### Topological Sort
- Орієнтований ациклічний граф (DAG) з 5-6 вершинами

### SCC
- Орієнтований граф з циклами та декількома компонентами

### PageRank
- Орієнтований граф з кількома вершинами (мінімум 5)

### Bridges & Articulation Points
- Неорієнтований граф з мостами (наприклад, дерево або граф з вузькими місцями)

### A*
- Граф з позиціями вершин на площині для евристики

### Kruskal
- Неорієнтований зв'язний граф з вагами

### Bellman-Ford
- Граф з від'ємними вагами (для демонстрації переваг над Дейкстрою)
