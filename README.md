# Graph Editor

Інтерактивний редактор графів з підтримкою алгоритмів теорії графів.

## Особливості

- 🎯 Створення та редагування направлених та ненаправлених графів
- 🔢 Алгоритми: MST (Prim), найкоротший шлях (Dijkstra), Ейлерові та Гамільтонові цикли
- ↩️ Undo/Redo функціональність
- 📐 Прив'язка до сітки
- 💾 Імпорт/експорт графів у JSON форматі
- 🎨 Візуалізація результатів з підсвіткою

## Технології

- **Cytoscape.js** - бібліотека для візуалізації графів
- **ES6 Modules** - модульна архітектура з import/export
- **MathJax** - відображення математичних формул
- **Canvas API** - малювання сітки

## Архітектура

Проєкт використовує **ES6 модулі** з чіткою структурою:

```
src/
├── core/
│   └── GraphManager.js          # Управління графом
├── algorithms/
│   └── GraphAlgorithms.js       # Алгоритми
├── managers/
│   ├── HistoryManager.js        # Undo/Redo
│   ├── GridManager.js           # Сітка
│   ├── ImportExportManager.js   # Імпорт/експорт
│   └── UIManager.js             # Інтерфейс
└── main.js                      # Точка входу
```

Детальний опис архітектури: [ARCHITECTURE.md](ARCHITECTURE.md)

## Запуск проєкту

### Варіант 1: Локальний сервер (рекомендовано)

Через обмеження CORS для ES6 модулів, потрібен локальний сервер:

```bash
# Python 3
python -m http.server 8000

# Node.js (встановіть http-server глобально)
npx http-server -p 8000

# PHP
php -S localhost:8000
```

Відкрийте браузер: `http://localhost:8000`

### Варіант 2: Розширення для VS Code

Встановіть розширення "Live Server" та запустіть проєкт.

### Варіант 3: Відкрити напряму (може не працювати)

Деякі браузери блокують ES6 модулі при відкритті файлу напряму (`file://`). У такому разі використовуйте варіант 1 або 2.

## Використання

1. **Додавання вузлів**: Натисніть "Add Node" → клік по полотну
2. **Додавання ребер**: Натисніть "Add Edge" → перетягніть від одного вузла до іншого
3. **Редагування ваги**: Подвійний клік по ребру
4. **Обчислення MST**: Натисніть "Calculate MST"
5. **Найкоротший шлях**: Введіть вузли → натисніть "Find Path"
6. **Експорт графа**: Натисніть "Export" → зберігається JSON файл
7. **Імпорт графа**: Натисніть "Import" → виберіть JSON файл

## Алгоритми

- **MST (Minimum Spanning Tree)** - алгоритм Пріма
- **Shortest Path** - алгоритм Дейкстри
- **Euler Trails/Circuits** - перевірка існування Ейлерових шляхів
- **Hamiltonian Cycles** - пошук Гамільтонових циклів
- **All Spanning Trees** - генерація всіх остовних дерев

## Розробка

### Структура класів

```javascript
// Імпорт модулів
import { GraphManager } from './core/GraphManager.js';
import { GraphAlgorithms } from './algorithms/GraphAlgorithms.js';
// ...

// Використання
const graphManager = new GraphManager('cy', config);
const algorithms = new GraphAlgorithms(graphManager.cy, true);
```

### Додавання нового алгоритму

1. Додайте метод у `GraphAlgorithms.js`
2. Створіть обробник у `UIManager.js`
3. Додайте кнопку в `index.html`

### Debugging

Всі модулі доступні через консоль:

```javascript
// У консолі браузера
window.GraphEditorApp.graphManager
window.GraphEditorApp.algorithms
// ...
```

## Ліцензія

MIT

## Автори

- Оригінальна версія: Graph Constructor
- Рефакторинг на ES6 модулі: 2024
